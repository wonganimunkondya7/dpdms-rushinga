spring: { application: { name: alert-service }, datasource: { url: '${ALERT_DB_URL:jdbc:mysql://localhost:3306/dpdms_alert}', username: '${MYSQL_USER:root}', password: '${MYSQL_PASSWORD:}' }, jpa: { hibernate: { ddl-auto: validate } }, sql: { init: { mode: always } } }
server: { port: 8087 }
eureka: { client: { service-url: { defaultZone: '${EUREKA_URL:http://localhost:8761/eureka/}' } } }
