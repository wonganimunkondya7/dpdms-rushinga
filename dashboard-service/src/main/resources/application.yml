spring: { application: { name: dashboard-service }, thymeleaf: { cache: false } }
server: { port: 8090 }
management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - metrics
